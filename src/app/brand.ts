/**
 * Single source of product naming so a rename touches one file (§1).
 */
export const BRAND = {
  name: 'POSE',
  tagline: 'Photograph people better',
  shortDescription:
    'Find a pose that suits where you are, see what it should look like, then line it up through the camera.',
} as const;
